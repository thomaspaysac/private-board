extends layout

block content
  h1= title

  if currentUser 
    p Be welcome, #{currentUser.username}.
    div 
      a(href='/user/log-out') Log out
  else 
    p
      a(href='/user/log-in') Log in
      |  or 
      a(href='/user/sign-up') Sign up 
      |  to start using the board.

  div.background
    div.message-display_outer
      div.messages-display
        if messages.length === 0
          div No message yet. Be the first! 
        else
          each message in messages
            div.message-container
              div.message_user= message.author.username
              div.message_text= message.text
    a.new-message_link(href='/new') New message